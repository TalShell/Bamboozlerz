#! /usr/bin/env python3

from library import Library
from book import Book

def process(books, libs, days):
    explored_libs=[]
    startD=0;
    for l in libs:
        startD += l.signup_days
        l.sinupDate = startDate
    
    
    
